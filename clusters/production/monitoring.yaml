apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: monitoring
  namespace: flux-system
spec:
  dependsOn:
    - name: infra-configs
  interval: 1h
  retryInterval: 2m
  timeout: 5m
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./monitoring/production
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  postBuild:
    substitute:
      # https://github.com/prometheus-community/helm-charts/releases
      prometheus_version: "86.1.1"
      # https://github.com/grafana-community/helm-charts/releases
      loki_version: "17.1.7"
      # https://github.com/grafana/helm-charts/releases?q=alloy
      alloy_version: "1.8.2"
      # https://github.com/seaweedfs/seaweedfs/releases
      seaweedfs_version: "4.32.0"
