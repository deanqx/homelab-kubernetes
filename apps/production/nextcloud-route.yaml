apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: user-route
  namespace: nextcloud
spec:
  parentRefs:
  - name: cilium
    namespace: cilium-gateway-system
    sectionName: https
  hostnames:
  - "cloud.kowi.it"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: nextcloud
      port: 80
