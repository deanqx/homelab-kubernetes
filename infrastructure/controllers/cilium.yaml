apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: cilium
  namespace: kube-system
spec:
  interval: 24h
  url: oci://quay.io/cilium/charts/cilium
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  ref:
    semver: "${cilium_version}"
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cilium
  namespace: kube-system
spec:
  interval: 12h
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
      # Automatically rollback on failure
      remediateLastFailure: true
  chartRef:
    kind: OCIRepository
    name: cilium
    namespace: kube-system
  values:
    cluster:
      name: default
    # points to control plane
    k8sServiceHost: dean-homelab
    k8sServicePort: 6443
    # bypasses iptables, use eBPF instead
    kubeProxyReplacement: true
    bpf:
      # Source NAT for traffic going out of the cluster
      # uses node ip for traffic
      # could be disabled when BGP is enabled
      masquerade: true
    # required for routing between different locations
    routingMode: tunnel
    tunnelProtocol: vxlan
    # temporary solution until BGP is used
    l2announcements:
      enabled: true
    # IP Address Management
    ipam:
      operator:
        clusterPoolIPv4PodCIDRList:
          - 10.42.0.0/16
        clusterPoolIPv4MaskSize: 24
    gatewayAPI:
      enabled: true
    hostFirewall:
      enabled: true
    operator:
      replicas: 1
